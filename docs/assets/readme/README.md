# README product captures

These files are public presentation copies of reviewed Performance Lab desktop product references.

They are deliberately stored outside `design/reference/visual-goldens/` so the public README does not depend directly on visual-test expectation paths. The visual goldens remain test evidence; these copies remain README presentation assets and should be refreshed deliberately when the public product story changes.

Current source mapping:

- `find-best-setup.png` <- `design/reference/visual-goldens/desktop-standard/campaign-results.png`
- `test-a-model.png` <- `design/reference/visual-goldens/desktop-standard/test-a-model-review.png`
- `sample-evidence.png` <- `design/reference/visual-goldens/desktop-standard/sample-evidence-detail.png`

These deterministic captures demonstrate product UI/state only. They do not constitute representative physical-device performance evidence.
